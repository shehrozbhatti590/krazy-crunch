// Delivery fee applied to POS orders where order type = "delivery".
// Currently 0 (free delivery). Change this single number any time you want
// to start charging for delivery - no other code needs to change.
export const DEFAULT_DELIVERY_FEE = 0;
